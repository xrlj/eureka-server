eureka:
  instance:
#    instance-id: ${spring.application.name}:${spring.cloud.client.ip_address}:${server.port}
#    hostname: eureka.xrlj.com
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://192.168.0.48:15671/eureka/
